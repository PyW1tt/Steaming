// import { Router } from "express";
// import protect from "../middleware/protect";

// const adminRouter = Router();
// adminRouter.use(protect);

// adminRouter.get("", async (res, req) => {});

// export default adminRouter;
